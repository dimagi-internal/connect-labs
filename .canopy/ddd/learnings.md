# DDD Learnings — create-survey-solicitation

- [create-survey-solicitation] iter0 dual-judge FAIL (concept 2.0 / user 2.0), auto=stop_unclear. Gating finding: scene-3 capture is the solicitation DETAIL page (/solicitations/4495/) but the declared coverage-checkbox selector lives on /respond/ — capture stops one click short. fix_kind=options (re-point scene url vs rewrite narration).
- [create-survey-solicitation] response #4496 (sol 4495, opp 10008 labs-only): set org_name=Health Bridge Nigeria, submitted_by_name=Amina Okafor, submitted_by_email=amina.okafor@healthbridge.ng. MCP solicitation tools (get_solicitation/list_responses/get_response) hit the PROD labs_record API and 404 for labs-only opps (id>=10000); they don't pass program_id to engage the local backend. To read/write a labs-only solicitation response, run server-side via `aws ecs run-task` (containerOverrides: python manage.py shell -c ...) using SolicitationsDataAccess(program_id="10008", access_token="<dummy>") — the dummy token is required by **init** but never used for labs-only (it short-circuits to the local records backend, no HTTP/permission checks).
- [create-survey-solicitation] recorder note: scenes with no `actions` (navigation+capture only) need `--snapshot-empty-scenes` or the snapshots dir stays empty (no PNG/page_text for the judges). Do NOT pass --skip-empty-scenes for an all-navigation spec — it drops every scene ("no scenes resolved").

- [2026-06-18T15:40:00Z] create-survey-solicitation iter4 (RESUME convergence pass): maps + scene-1 finished-state + award scene all LIVE & verified.

  - Data: re-seeded R6 plan 4494 → approved, group 4492 → reviewed; refreshed sol #4495 snapshot with Attakar(green)/Gura(blue) boundary geometry; re-opened #4495 to status=active. Response #4496 (Health Bridge Nigeria / Amina Okafor, coverage 4494) preserved.
  - GOTCHA: local backend update_record does FULL data replace (row.data = data or {}), NOT merge — a partial update_solicitation({status}) WIPED plans. Must send the COMPLETE solicitation data dict on every update for labs-only records.
  - GOTCHA: respond/detail/create maps only render when web reads the labs-only record via program_id context (SolicitationsDataAccess(program_id=10008) → local backend); web's get_solicitation_by_id(public=True) hits PROD api and 401s/returns empty for labs-only. Scene URLs need ?program_id=10008.
  - GOTCHA: recorder needs --snapshot-empty-scenes for navigation-only scenes (1,2,4,5 here) or only action-scenes get a PNG/page_text. Do NOT use --skip-same-url for a multi-page spec — it reused STALE prior-iter snapshots (scene_5 captured old /responses/ not the new award URL).
  - Product PR #634: portfolio study-card counts real wards (ward_count = named input_areas) not plan count ("2 wards" not "1 wards"); "ready to solicit" green chip+ring + primary Create solicitation when members approved & group reviewed/exported; respond AI-coach panel gated on question_fields (no orphaned "review your draft answers" on coverage-only apps).
  - Scene 5: 2/5 → 4/5 (re-point to real Award page resolved byte-identical-frame). Scenes 2/3 maps verified rendering real polygons.
  - STALL: concept 2/5, user 2/5, score_history [2,2,2] → stop_max_iter. Weakest-link MIGRATED (scene-1 contradiction → scene-1 portfolio SEED-NOISE: "Untitled study/0 wards" placeholder + dense Draft grid + "Deploy →" stealing primary color). Also award page raw Connect-org-ID free-text field (no picker) = user-judge task_completion 2. All 3 concept findings mechanical.
    [gate-tracking] class=stop_max_iter decision=surfaced run=create-survey-solicitation-2026-06-17-001 (scene-1 portfolio seed-noise; award org-ID picker)

- [2026-06-18T16:05:00Z] create-survey-solicitation iter5 (after #636 suppress empty-study placeholder): concept 2→3 (WARN), user 2.0 (FAIL, composition shifted). score_history [2,2,2,2]. auto=stop_unclear (5 options/redesign findings — need user pick).

  - Scene-1 visual_polish 2→3 (placeholder gone). Remaining scene-1 blockers are NOT mechanical: (a) redesign — finished R6 study buried under the 8-card Draft PLANNING grid (portfolio hierarchy leads with plans not studies); (b) options — "Create solicitation" CTA on every Draft card (hide vs disable vs only-on-approved).
  - User-judge floor is a CLARITY/jargon pattern recurring across scenes: raw "R6 — Attakar × Gura" coded label (vs plain "Attakar & Gura"), "EOI" badge unexpanded, "Organization ID" pre-filled with a slug not a name, no lifecycle legend. De-jargoning user-facing labels would lift 4 scenes off the floor at once — but it's a content/label-direction call, surfaced.
  - Scene-5 award honesty (options): the deferred live-opp-provisioning boundary (G1) is narration-only, not surfaced in-UI (stepper / disabled "coming soon" chip).
    [gate-tracking] class=stop_unclear decision=surfaced run=create-survey-solicitation-2026-06-17-001 (portfolio hierarchy redesign; Create-solicitation-on-Draft options; de-jargon labels; award G1 in-UI)

- [2026-06-18T18:18:37Z] iter3 dual-judge FAIL (concept 2.0 / user 2.0), auto=stop_max_iter (score stalled [2.0,2.0,2.0]). DATA FIX LANDED: plan 4494 draft->in_review->approved via ProgramPlanDataAccess('10008', access_token='dummy').transition_plan over aws ecs run-task (labs-jj-web:242, subnet-08a18eb47b48aff54, sg-0666a5ed512c97d9d, assignPublicIp=ENABLED) — dummy token required by **init** but unused for labs-only (opportunity_id auto-set->local backend). Scene-1 green 'READY TO SOLICIT' hero now CONFIRMED live + browser-verified (replaces prior 'Untitled draft list' failure: scene1 concept 2->4). Scene 3 now correctly captures /respond/ map+coverage (prior iter stopped one click short). NEW binding weakest link: scenes 4&5 award-state incoherence (STATUS 'Awarded' beside RECOMMENDATION 'Not reviewed' + live Review action + duplicate Awarded label) and scene5 byte-identical to scene4 (motion_friction). All 11 findings fix_kind=mechanical but score not converging -> human look. Recorder note CONFIRMED: all-nav spec needs --snapshot-empty-scenes (NOT --skip-empty-scenes); --storage-state ~/.ace/labs-session.json works for session auth.
- [2026-06-18T18:18:37Z] upload.py (canopy 0.2.205) reads unified_spec.yaml + why_brief.yaml FROM the run dir — copy docs/walkthroughs/<slug>.yaml -> <run_dir>/unified_spec.yaml before ddd upload. walkthrough-share/upload.py in 0.2.205 uses --feature (not --narrative-slug) and rejects --companion-url (truncated arg build); role/run-id/feature/narrative-review-id flags work.
- [2026-06-18T20:48:01Z] iter5 (create-survey-solicitation): PR #647 scene-4 awarded-coherence fix CONFIRMED live (RECOMMENDATION ->; ACTIONS Awarded muted status not button; header Awarded badge not Active). User-artifact scene-4 clarity 3->4. RESOLVED.
- [2026-06-18T20:48:01Z] RECORDER GOTCHA: static (action-empty) scenes 1/2/4 are NOT snapshotted by default. --skip-empty-scenes DROPS them from the loop entirely (stale snapshots), and even without it, action-empty scenes get no snapshot unless --snapshot-empty-scenes is passed. For a DDD full re-render where every scene must be re-captured (esp. ones carrying a fix), pass --snapshot-empty-scenes and DROP --skip-empty-scenes.
- [2026-06-18T20:48:01Z] iter5 did NOT converge (concept 2 fail / user 3 fail, bar 4.0). REGRESSION from iter4 (3): the arm-legend PR #647 added to scenes 2/3 is mispositioned — the Comparison legend row is clipped and overlaps the Mapbox attribution wordmark, dragging concept visual_polish to 2 (S3). One-line CSS reposition fixes it. stop_unclear (1 options finding: S1 visual_polish dead-whitespace pick).
- [2026-06-18T21:16:54Z] create-survey-solicitation iter6: coverage-map arm legend regression RESOLVED via PR #648 (legend bottom-left -> top-left, off the Mapbox logo). Scene 3 recovered: concept visual_polish 2->4, concept_clarity 3->4; user-artifact scene 3 -> 4.00 (tied highest). Run did NOT converge (concept 3.0 / user 3.25 vs bar 4.0); stop_unclear on 1 options finding (scene 3 design_soundness: map<->checklist single-row affordance + submit-gating signal needs a user pick).

- [2026-06-18T17:31Z] iter7 CAPTURE-MOTION fix (the demo was near-static): scene 5's award click FAILED (3 failed actions: wait_for/fill/click on reward_budget timed out) because response #4496 was in the AWARDED end-state, so the award page rendered the CONFIRMATION not the FORM — AND it made scene 4 temporally wrong (Maya saw an already-awarded response before awarding). Scenes 1/2/4 were show:-only (no cursor motion).
  - FIX 1 — per-render reset: response #4496 -> status=submitted + clear reward_budget/awarded_at/awardee_notified/org_id, so the award FORM (budget input + Award button) re-renders. Scene 5's click RE-AWARDS #4496, so this MUST run before every render. Wired as a spec `setup:` block (`command: python scripts/walkthroughs/create-survey-solicitation/reset_award.py`, `rerun: per_render`, no `outputs:` — spec_qa only requires setup.outputs when scenes use ${...} placeholders, and this spec uses literal URLs). The recorder runs the setup block itself before filming (don't pre-run; never pass --skip-setup). reset_award.py fires `aws ecs run-task` (labs-jj-web latest rev, subnet-08a18eb47b48aff54, sg-0666a5ed512c97d9d, assignPublicIp=ENABLED, container `web`, log group /ecs/labs-jj-web, stream web/web/<task-id>) running SolicitationsDataAccess(program_id='10008', access_token='dummy').update_response — FULL data replace, so it re-sends the whole #4496 dict with only the award fields cleared (preserves selected_plan_ids=[4494], Health Bridge Nigeria, Amina). MCP solicitation tools 404 for labs-only opps; ECS is the only transport.
  - FIX 2 — cursor motion on static scenes via non-destructive `hover`/`scroll_to` (recorder kind `hover` glides the cursor overlay onto the target): scene1 -> hover the R6 group's `a.primary-dark:has-text("Create solicitation")` CTA (no click; clicking navigates off the portfolio); scene2 -> hover `#sol-coverage-map` then scroll_to+hover `button[type=submit]:has-text("Create Solicitation")` (NO click; clicking would dup #4495); scene4 -> hover the `span:has-text("R6 — Attakar")` coverage chip + the `a:has-text("Award")` action. YAML gotcha: a `#4495` mid an UNQUOTED plain scalar `note:` starts a comment and breaks parsing — quote notes containing `#`.
  - RESULT: render 20/20 actions OK, 0 failed (was 10 actions / 3 failed). Scene 5 award form renders -> fill+click SUCCEED -> live green "Awarded to Health Bridge Nigeria — has been notified" confirmation + ₦500,000 + honest "Next step — not yet built" panel. Scene 4 now Submitted (pre-award), temporally correct. Dual-judge: concept 3.0 warn / user 3.25 warn (bar 4.0) — DID NOT converge; remaining 3-floor is visual_polish + motion_friction (static-frame cursor occluding action labels; dead lower-viewport whitespace) NOT a broken flow. Scene 5 scored its best yet (concept_clarity 5, why_groundedness 5, claim_reality_coherence 5). stop_unclear (options/redesign findings: scene-3 map-legend<->checklist vocabulary reconcile). Refreshed --stuck review package (phase stays `judged`, iterable; NOT externally published).
