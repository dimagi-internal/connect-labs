# DDD Learnings — create-survey-solicitation

- [create-survey-solicitation] iter0 dual-judge FAIL (concept 2.0 / user 2.0), auto=stop_unclear. Gating finding: scene-3 capture is the solicitation DETAIL page (/solicitations/4495/) but the declared coverage-checkbox selector lives on /respond/ — capture stops one click short. fix_kind=options (re-point scene url vs rewrite narration).
- [create-survey-solicitation] response #4496 (sol 4495, opp 10008 labs-only): set org_name=Health Bridge Nigeria, submitted_by_name=Amina Okafor, submitted_by_email=amina.okafor@healthbridge.ng. MCP solicitation tools (get_solicitation/list_responses/get_response) hit the PROD labs_record API and 404 for labs-only opps (id>=10000); they don't pass program_id to engage the local backend. To read/write a labs-only solicitation response, run server-side via `aws ecs run-task` (containerOverrides: python manage.py shell -c ...) using SolicitationsDataAccess(program_id="10008", access_token="<dummy>") — the dummy token is required by **init** but never used for labs-only (it short-circuits to the local records backend, no HTTP/permission checks).
- [create-survey-solicitation] recorder note: scenes with no `actions` (navigation+capture only) need `--snapshot-empty-scenes` or the snapshots dir stays empty (no PNG/page_text for the judges). Do NOT pass --skip-empty-scenes for an all-navigation spec — it drops every scene ("no scenes resolved").
- [2026-06-18T13:48:59Z] create-survey-solicitation iter2: responses_list.html table clipped the ACTIONS/Award column at 1280px (overflow-hidden on a 7-col min-w-full table). Fix=wrap in overflow-x-auto. Shipped #629.
- [2026-06-18T13:48:59Z] create-survey-solicitation iter2: respond.html gated Application Questions card on form.fields (truthy even with only select_plans) -> empty stub when no custom questions. Fix=gate on question_fields. Shipped #629.
- [2026-06-18T13:48:59Z] create-survey-solicitation iter2: user-artifact judge PASS 4/5 after re-point fixes (scene1 portfolio, scene3 /respond/, scene2 coverage card, scene4 realistic email + n=1 declutter). Prior 2/5.
- [2026-06-18T13:48:59Z] create-survey-solicitation iter2 OPEN (needs human direction): (a) scene-1 portfolio buries the finished R6 study under 7+ Untitled/0-ward placeholder cards -> CONCEPT/redesign, surface ready-to-solicit cue; (b) scene-5 reuses scene-4 frame byte-identical -> CONCEPT/options, re-point to an Award-focused look-ahead capture. These hold concept at 2/5.
  [gate-tracking] class=concept_change decision=surfaced run=create-survey-solicitation-2026-06-17-001 (scene-1 portfolio direction + scene-5 frame-distinction)
- [2026-06-18T14:13:28Z] create-survey-solicitation iter3: STALL at concept 2/5 (score_history [2,2,2]). Two iter-2 mechanical fixes (#629 table overflow, #630 padding; scene-3 empty-questions gate) VERIFIED resolved in capture, but scene-1 became the weakest-link (4/5 gating dims): portfolio capture CONTRADICTS the narrative opening (R6 study reads Draft/defining/1 wards, not finished). Highest-leverage fix = re-seed R6 study+plan into finished/approved state. Remaining loop blocker is NOT mechanical CSS -> stop_unclear.
  [gate-tracking] class=stop_unclear decision=surfaced run=create-survey-solicitation-2026-06-17-001 (scene-5 frame-distinction options; scene-1 finished-state precondition)
