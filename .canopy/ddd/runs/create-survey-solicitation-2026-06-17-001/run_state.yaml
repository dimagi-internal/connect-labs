auto_iterate_next_action: stop_unclear
auto_iterate_reason: 2 options/redesign finding(s) — need a user pick.
findings:
  - detail:
      A 5-second glance returns a wall of undifferentiated 'Draft' cards — two
      literally named 'Attakar / Attakar' — so the hero 'finished plan -> one-click
      solicitation' story is not findable; the opening fails to establish the problem.
    dimension: concept_clarity
    fix_kind: mechanical
    fix_recommendation:
      Lead the portfolio with the one finished R6 study in a distinct
      'ready-to-solicit' lane with a one-line problem framing, so the scene's purpose
      reads in 5 seconds.
    route: CONCEPT
    route_url: /microplans/program/10008/
    scene: scene_1 — Maya turns her finished study into a call for a firm
    severity: medium
  - detail:
      "'Create solicitation' has zero primacy — it is the 3rd of four equal-weight
      text actions (Archive * Submit for review * Create solicitation * Delete) on every
      card, visually identical to Delete; the LIVE lane is an empty dead end and R1-R4
      cards are near-empty."
    dimension: design_soundness
    fix_kind: mechanical
    fix_recommendation:
      Promote 'Create solicitation' to a single styled primary button
      and demote Archive/Submit/Delete into a '...' overflow; surface it primarily on
      the finished study.
    route: PRODUCT
    route_url: /microplans/program/10008/
    scene: scene_1 — Maya turns her finished study into a call for a firm
    severity: medium
  - detail:
      "Fixture noise: duplicate 'Attakar / Attakar' cards, '1 wards' (broken
      pluralization that contradicts the page's own intervention+control study definition),
      a ragged grid of full-vs-empty cards, and faint low-contrast area/status metadata."
    dimension: visual_polish
    fix_kind: mechanical
    fix_recommendation:
      Suppress/de-dupe the seed-noise cards, fix singular/plural ('1
      ward'), normalize card density, and raise contrast on area-count + status metadata.
      Product/seed-data fix, never a camera crop.
    route: PRODUCT
    route_url: /microplans/program/10008/
    scene: scene_1 — Maya turns her finished study into a call for a firm
    severity: medium
  - detail:
      Spine S3 rests on 'the approved plan' / 'a FINISHED micro-plan', but every
      visible plan card reads 'Draft' and the R6 study reads 'defining * 1 wards' —
      the finished-plan precondition the concept_claim depends on is contradicted by
      the still.
    dimension: why_groundedness
    fix_kind: mechanical
    fix_recommendation:
      Re-seed/re-capture this scene with the R6 study in an Approved/Reviewed/finished
      state (and a ward count satisfying the intervention+control definition) so the
      'launch from a finished plan' claim is visible, not just the button.
    route: RESEARCH
    route_url: /microplans/program/10008/
    scene: scene_1 — Maya turns her finished study into a call for a firm
    severity: medium
  - detail:
      Strong seed-data smell in a single still — duplicate-named cards, '1 wards',
      empty R1-R4 cards, and an empty 'LIVE ... none' lane read as a half-populated
      fixture rather than a clean capture.
    dimension: motion_friction
    fix_kind: mechanical
    fix_recommendation:
      Seed a realistic, de-duplicated portfolio with at least one
      finished study (and suppress empty placeholder lanes) before capture.
    route: PRODUCT
    route_url: /microplans/program/10008/
    scene: scene_1 — Maya turns her finished study into a call for a firm
    severity: medium
  - detail:
      ADVISORY (non-blocking). The 'launch from a finished plan' narration is
      not demonstrated — the Create-solicitation button sits on Draft cards and a 'defining
      / 1 wards' study; '1 ward' also contradicts the page's own intervention+control
      definition.
    dimension: claim_reality_coherence
    fix_kind: mechanical
    fix_recommendation:
      Show the button on a genuinely finished study whose ward count
      satisfies the intervention+control definition.
    route: DEFER
    route_url: /microplans/program/10008/
    scene: scene_1 — Maya turns her finished study into a call for a firm
    severity: medium
  - detail:
      What+why is legible (create form pre-attaches snapshotted coverage) but
      NO concrete user/role is named on screen — no owner identity/persona label — which
      caps concept_clarity at 3.
    dimension: concept_clarity
    fix_kind: mechanical
    fix_recommendation:
      Name the planner on screen (an owner avatar/byline, e.g. 'Maya
      — Program Planner') so the role anchors the 'why'.
    route: CONCEPT
    route_url: /solicitations/create/?source_program_id=10008&source_group_id=4492
    scene: scene_2 — Maya finds the plan already waiting as the coverage area
    severity: low
  - detail:
      The Coverage-areas panel is a coherent, clearly-attached affordance (green
      'Attached' badge, 'Snapshotted at creation' note, 2 wards / 840 work areas / study
      arms), but the surrounding form is a sea of empty required-looking fields (deadline,
      contact email) with no required/optional distinction, and two empty sections (Questions,
      Criteria) give no preview.
    dimension: design_soundness
    fix_kind: mechanical
    fix_recommendation:
      Pre-fill or visibly mark required fields and de-emphasize optional
      ones — or capture one step later with key fields filled so the form reads as a
      confirm-and-publish moment, not a blank mid-fill.
    route: PRODUCT
    route_url: /solicitations/create/?source_program_id=10008&source_group_id=4492
    scene: scene_2 — Maya finds the plan already waiting as the coverage area
    severity: low
  - detail:
      Clean type hierarchy and consistent controls, but the middle third is dominated
      by empty placeholder inputs (whitespace reads as 'unfinished'), and the coverage
      card's mini-map is a generic pin-in-a-square glyph rather than an actual map of
      the 2 wards / 840 work areas it claims.
    dimension: visual_polish
    fix_kind: mechanical
    fix_recommendation:
      Replace the coverage card's generic pin glyph with a real mini-map
      of the wards, and reduce the empty-placeholder dominance by pre-filling/marking
      required fields.
    route: PRODUCT
    route_url: /solicitations/create/?source_program_id=10008&source_group_id=4492
    scene: scene_2 — Maya finds the plan already waiting as the coverage area
    severity: low
  - detail:
      Clean static frame with no render artifacts, but the all-empty mid-fill
      form state reads as mid-interaction rather than a settled confirm step; cursor
      parked oddly in the breadcrumb whitespace.
    dimension: motion_friction
    fix_kind: mechanical
    fix_recommendation:
      Capture the frame in a more settled (partly-filled) state and
      park the cursor off the breadcrumb text.
    route: PRODUCT
    route_url: /solicitations/create/?source_program_id=10008&source_group_id=4492
    scene: scene_2 — Maya finds the plan already waiting as the coverage area
    severity: low
  - detail:
      The 'Which areas can you cover? — Select one or more' prompt renders exactly
      ONE checkbox (a degenerate, near-contradictory affordance), and the AI-coach card
      promises to review 'your draft answers against the evaluation criteria' for which
      no answer fields exist on the page — a capability/UI mismatch.
    dimension: design_soundness
    fix_kind: mechanical
    fix_recommendation:
      Relabel the single-area case honestly (or surface the real sub-areas
      so 'one or more' is true), and scope the AI-coach copy to what the page actually
      contains (the coverage selection), not 'draft answers'.
    route: PRODUCT
    route_url: /solicitations/4495/respond/
    scene: scene_3 — Amina reads the open call and ticks the area she can staff
    severity: medium
  - detail:
      Two saturated filled CTAs ('Get Feedback on My Answers' and 'Submit Response')
      collide for primary status, and ~40% of the frame is dead whitespace with the
      form floating in the upper third — a balance/hierarchy failure a careful viewer
      catches immediately.
    dimension: visual_polish
    fix_kind: mechanical
    fix_recommendation:
      Demote 'Get Feedback on My Answers' to a secondary/outline button
      and constrain/center the form card to remove the dead lower whitespace. Product
      change, never a camera crop.
    route: PRODUCT
    route_url: /solicitations/4495/respond/
    scene: scene_3 — Amina reads the open call and ticks the area she can staff
    severity: medium
  - detail:
      States what+why with a concrete role (survey firm) and clear problem (apply
      by picking coverage), but the single-option selector and the AI card referencing
      nonexistent 'answers' blur the one-sentence read.
    dimension: concept_clarity
    fix_kind: mechanical
    fix_recommendation:
      Give the coverage option a distinct, self-describing label (geography
      + scope) so the choice is legibly a coverage AREA, not an echo of the solicitation
      title.
    route: CONCEPT
    route_url: /solicitations/4495/respond/
    scene: scene_3 — Amina reads the open call and ticks the area she can staff
    severity: low
  - detail:
      ADVISORY (non-blocking). The AI-coach card claims to review 'your draft
      answers against the evaluation criteria', but the page exposes no answer fields/questions
      — only a single coverage checkbox; nothing for the coach to review as captured.
      The 'blocked until chosen' error string is correctly not visible (fires on submit)
      and was not deducted.
    dimension: claim_reality_coherence
    fix_kind: mechanical
    fix_recommendation:
      Make the coach copy reference what's actually on the page (the
      coverage selection), or add the answer fields the coach implies.
    route: DEFER
    route_url: /solicitations/4495/respond/
    scene: scene_3 — Amina reads the open call and ticks the area she can staff
    severity: medium
  - detail:
      ACTIONS header is complete and all three actions (View / Review / Award
      trophy) are fully in-frame (iter-2 clipping RESOLVED), but green is over-used
      across the coverage pill, the SELECTED-COVERAGE check, the 'Active' status dot,
      AND the Review icon — four greens with four unrelated meanings flatten the signal.
    dimension: visual_polish
    fix_kind: mechanical
    fix_recommendation:
      Reserve green for a single meaning (status). Make the coverage
      pill a calm neutral/indigo and use one consistent action accent with the primary
      highlighted.
    route: PRODUCT
    route_url: /solicitations/4495/responses/?program_id=10008
    scene: scene_4 — Maya sees the firm tied to the coverage she designed
    severity: low
  - detail:
      The beat must ADD the look-ahead/honesty about provisioning-as-next-step,
      but the frame is a near-duplicate of scene 4's responses table (byte-identical
      capture) with NO distinct on-screen affordance for the Award boundary — the 5-second
      read is 'same responses screen', a 'nothing new happened' result.
    dimension: concept_clarity
    fix_kind: options
    fix_recommendation:
      Either re-point scene 5 to a genuinely distinct capture (the
      Award control focused/hovered with an aspirational 'provision a live opportunity
      — next step' treatment) or give the final beat a distinct on-screen affordance.
      Surface to the user; do not auto-apply.
    route: CONCEPT
    route_url: /solicitations/4495/responses/?program_id=10008
    scene: scene_5 — Maya looks ahead to standing up the real opportunity
    severity: medium
  - detail:
      The sequence does not visibly ADVANCE — this final beat is near-identical
      to (byte-identical capture of) the immediately prior responses scene with no distinct
      affordance, hover, focus, or aspirational treatment on the Award control, so it
      reads static.
    dimension: motion_friction
    fix_kind: options
    fix_recommendation:
      Differentiate the final frame from scene 4 with a state change
      on Award (focus/hover/annotation) so the viewer perceives a deliberate forward
      step into the look-ahead. Surface to the user; do not auto-apply.
    route: PRODUCT
    route_url: /solicitations/4495/responses/?program_id=10008
    scene: scene_5 — Maya looks ahead to standing up the real opportunity
    severity: medium
  - detail:
      ADVISORY (non-blocking). The 'Award as next-step boundary' look-ahead is
      narrated but NOT visible/distinct in the still — the Award control is identical
      to two fully-working actions with no boundary treatment. The honest deferral of
      opportunity-provisioning (G1) is correct behavior and was NOT penalized.
    dimension: claim_reality_coherence
    fix_kind: options
    fix_recommendation:
      Optionally surface the deferral in-frame (a small 'provisioning
      a live opportunity — not yet built' note near Award) so the still carries the
      S7 provenance rather than only the voiceover.
    route: DEFER
    route_url: /solicitations/4495/responses/?program_id=10008
    scene: scene_5 — Maya looks ahead to standing up the real opportunity
    severity: medium
findings_review_id: null
findings_review_url: null
iteration: 3
iteration_clips: {}
iteration_decks: {}
last_actor: null
last_actor_at: null
narrative_review_id: acd53ae4-c44c-4c0a-98dd-f766b9358acc
narrative_review_url: /review/acd53ae4-c44c-4c0a-98dd-f766b9358acc/
narrative_slug: create-survey-solicitation
pending_review: null
phase: judged
run_id: create-survey-solicitation-2026-06-17-001
scene_filter: null
scenes_run:
  - 1
  - 2
  - 3
  - 4
  - 5
schema_version: 1
score_history:
  - 2.0
  - 2.0
  - 2.0
verdicts:
  concept: /Users/jjackson/emdash/worktrees/connect-labs/emdash/solicitation-rntwj/.canopy/ddd/runs/create-survey-solicitation-2026-06-17-001/verdict-concept.yaml
  user_artifact: /Users/jjackson/emdash/worktrees/connect-labs/emdash/solicitation-rntwj/.canopy/ddd/runs/create-survey-solicitation-2026-06-17-001/verdict-user.yaml
why_brief: null
