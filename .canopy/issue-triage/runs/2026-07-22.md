# Issue Triage — dimagi-internal/connect-labs — 2026-07-22

Triaged **6 of 6** open issues (no truncation). Code evaluated: local working tree
(branch `emdash/ready-items-smile-jdl4w`). One subagent per issue, read-only.

> Note: issues #627 and #674 (the "600s") were closed as not-planned earlier in the
> same session, before this run.

| #   | Disposition | Conf   | Effort | Title                                                      | Evidence / why                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | ----------- | ------ | ------ | ---------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 947 | investigate | —      | —      | Program-scoped labs-only reads 401 against real Connect    | Subagent traced api*client.py:93-99 / data_access.py:411 as \_correct* — but issue was filed 2026-07-21, **after** that code (fix #642 Jun 18, last touch #822 Jul 2), and the author explicitly quoted those lines as "looks correct" yet still saw the 401. Code-trace can't clear it. Likely a **registration/data** cause: `is_labs_only_program_id` does a DB lookup for `SyntheticOpportunity(labs_only=True)` — if program 10100 isn't registered as labs-only in that env it dispatches to prod → 401. Needs a repro.                                                                                       |
| 956 | investigate | —      | —      | mirror-mode generation flattens per-visit trajectories     | Code + passing tests (entities.py:93-116 `plan_mirror_visits`, engine.py:161-190, test_engine.py:195/286) show per-visit `forced_values` are applied and `manifest.py` routes through that engine. **But** fixtures unchanged since Jul 1 and issue filed Jul 22 with a concrete real-opp repro (879 → 10036, within-child MUAC spread = 0). Unit tests (synthetic climbing curve) pass while a real de-identified `transplant_pool` reportedly flattens → conflict the code alone can't settle. Needs a 879→10036 repro (check profiler populates `visits[].values` for that opp). May be a stale-deploy artifact. |
| 958 | investigate | medium | S      | pipeline 'int' transform is a silent no-op; add round      | Reported **premise is largely wrong**: `_transform_to_sql` (query_builder.py:368) wires int via `validated_int`/`simple_int` → `::INTEGER` (396-410), and the else branch **warns** (443-445), not silent. Ordinal integerizing already happens by field-kind (`fields.py:236`, `:386`). Real residual: there's no bare `int` alias / no `round`/`round_int`, and unknown transforms warn rather than reject loudly. Whether to keep depends on reporter intent (bare-`int` alias? fail-loud?) → scope clarification.                                                                                               |
| 957 | implement   | high   | S      | config_hash ignores linking_field (+ reads path not paths) | Confirmed: `get_config_hash` (labs/analysis/utils.py:37-73) never references `config.linking_field` (the ENTITY GROUP BY key, config.py:618/669) and reads only singular `field.path`/`hist.path` (utils.py:41,51) while fields can be defined via `paths` (config.py:203-208,283-287). Editing the grouping key or a `paths`-only field doesn't invalidate the entity-result cache. Fix: append `linking_field`; use `get_paths()`/fall back to `paths`.                                                                                                                                                           |
| 904 | implement   | medium | S      | completed audit doesn't disable bulk pass/fail controls    | **Partly already fixed**: bulk-action buttons already `:disabled="isReadOnly ..."` + `disabled:opacity-50` (bulk_assessment.html:212-229, isReadOnly at :1038) — the core "greyed/disabled" complaint is done. Residual only: bottom button label keys off `stats.pending` (:747), never swaps to "Save Notes" on a completed audit. One-line Alpine ternary.                                                                                                                                                                                                                                                       |
| 959 | implement   | high   | M      | sam_followup hardcodes child_case_id + colour/date fields  | Confirmed unaddressed: `linking_field: "child_case_id"` hardcoded (sam_followup.py:145,157), reads `form.fu_visit_date` only (:99), pre-baked `*_muac_colour` (:72-79,108-115), explicit `form.child_recovered` (:85,130) — no fallbacks to entity_name / visit_date / muac_cm-derived color+recovery. Mirror of real opp 879 collapses children to one blob. Fix template-side (schema fallbacks + muac_cm band derivation + e2e).                                                                                                                                                                                 |

## Recommended next actions (Phase 4.5)

- **#957 (implement, S)** — build it. Small, high-confidence, high-value (silent stale-cache footgun). Draft PR, `Closes #957`.
- **#904 (implement, S)** — build the residual button-label swap. `Closes #904`.
- **#959 (implement, M)** — build the template-side fallbacks. `Closes #959`. (Related to #956/#958's SAM/mirror context.)
- **#947 (investigate)** — repro: in labs shell, check `is_labs_only_program_id(10100)` and whether program 10100 is registered as `SyntheticOpportunity(labs_only=True)`; run `WorkflowDataAccess(program_id=10100).list_definitions()` and confirm the dispatch branch. Post findings; keep open with `needs-info`.
- **#956 (investigate)** — repro: `synthetic_profile_from_prod(879, mirror=true)` → `synthetic_generate_from_manifest` → opp 10036, then inspect per-child MUAC spread; check profiler populated `transplant_pool[i].visits[j].values`. Rule out stale deploy. Keep open with `needs-info`.
- **#958 (investigate)** — ask reporter to confirm intent: is a bare `int` transform alias needed (vs existing `simple_int`), and do they want unknown transforms to hard-fail? If yes → small `implement`. Post the "premise correction" + scope question; keep open.
