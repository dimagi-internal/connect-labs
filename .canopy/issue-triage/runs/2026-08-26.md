# Issue Triage — dimagi-internal/connect-labs — 2026-08-26

**Scope contract:** Triage + quick wins (cap: 5 `implement` items, effort S, fully local validation).
PR dispositions are outside the cap.

**Code evaluated:** local worktree fast-forwarded to `origin/main` = `b6cc3aca`.
**Coverage:** 20 of 20 open issues, 6 of 6 open PRs. No truncation.
**Carry-forward:** none applied. The prior run log (2026-07-22) is prose-only — no YAML
sidecar existed before this run — and all six issues it triaged (#904 #947 #956 #957 #958
#959) are already closed. Everything was re-triaged fresh; one subagent per issue, read-only.
This run writes the first sidecar, so the next run can carry forward.

**Tier counts:** blocks-e2e 8 · harness 4 · polish 8.

## Open PRs

| PR | Disp | Checks | Blocker / why | Title |
|---|---|---|---|---|
| 1282 | merge | linter✓ pytest✓ CodeRabbit✓ | none — CLEAN | workflow: add an "Every 2 weeks" schedule cadence |
| 1151 | merge | linter✓ pytest✓ CodeRabbit✓ | none — CLEAN | Address CodeRabbit review feedback on the KMC donation page |
| 953 | merge | linter✓ pytest✓ | none — CLEAN; `get_run()` still has no fallback on main | Fix: `WorkflowDataAccess.get_run()` misses opportunity-scoped runs under a program-scoped query |
| 895 | merge | linter✓ pytest✓ | none — CLEAN; `%-d` still present in 3 campaign call sites | fix: campaign date formatting + Windows GDAL detection cross-platform |
| 930 | unblock | linter✗ pytest✓ | linter red from a 2026-07-21 `pre-commit run --all-files` against that day's main. `.pre-commit-config.yaml:1` excludes `^\.canopy/`, so this docs-only diff cannot be the cause. Rebase on main → re-run. | docs(pm): delta-refresh Bulk Image Audit item map |
| 1186 | close | linter✓ pytest✓ | **superseded** — identical code merged as #1118 (`65a44cda`); `_REGIONS_BY_COUNTRY` + `country=org.country` are on main verbatim | supply: every supplier claimed to serve Nigeria and Sudan |

Five of six open PRs are mergeable or one rebase away.

## Clusters

- **mcp-aggregation-allowlist** — #1183 (canonical), #1317 (dup).
  One fix: `mcp/tools/pipelines.py` imports `VALID_AGGREGATIONS` from `labs/analysis/config.py`
  instead of restating it. The engine already implements median/mode/mode_share/dup_share with
  passing DB-backed tests, and `WORKFLOW_REFERENCE.md` is *correct*; the only rejecter is a
  stale hand-copied set at `pipelines.py:185` whose own comment falsely claims lockstep.
- **visit-level-pipeline-columns** — #1033, #1190 (both fixed by merged #1304 → close);
  #1198 and #1306 remain and are genuinely distinct layers (SQL SELECT list vs. serializer
  value precedence), not duplicates.
- **synthetic-repoint-invalidates-nothing** — #1034 (canonical), #1197.
  One edit site serves both: wire `FixtureStore.reload` + `SQLCacheManager.delete_all_cache`
  + `refresh_visit_count` into `synthetic_register` / `synthetic_repoint_by_source`. Both
  stay open — #1197's `visit_count` defect is a fourth cached artifact, not a duplicate.
- **labs-capacity-blocked-on-production-observation** — #1162, #1152, #1231, #1298.
  Not one code fix — one *validation visit*. See the validation queue below.

## Issues, by blocking tier

| # | Disp | Blocking | Conf | Effort | Cluster | Evidence / why |
|---|---|---|---|---|---|---|
| 1198 | implement | blocks-e2e | high | M | visit-columns | `query_builder.py:1532` SELECTs only 10 base columns; `flag_reason`/`date_created`/`review_status` never fetched though `RawVisitCache` stores them. Plus two disagreeing base-column frozensets (`config.py:63` vs `:400`) |
| 1034 | implement | blocks-e2e | high | M | synthetic-repoint | `synthetic.py:284` register calls only registry `invalidate_cache()`; `FixtureStore.reload`, `delete_all_cache`, `refresh_visit_count` all exist but are wired only into *other* paths |
| 1182 | implement | blocks-e2e | high | M | — | part (a) already shipped (#1185, `workflows.py:552`); part (b) open — `workflow_snapshots.py:55` hard-requires `opportunity_id`, so a program-owned run can be created but never concluded |
| 1231 | blocked | blocks-e2e | high | M | labs-capacity | `tasks.py:339` comment names #1231 as its own tracker; retry backoff (#1236) and saturation alarm (#1237) landed, cross-run budget did not |
| 1162 | blocked | blocks-e2e | med | M | labs-capacity | #1293 + #1321 landed today; cache-MISS path at `backend.py:215` still rebuilds the whole raw-visit partition inside the request |
| 1193 | blocked | blocks-e2e | med | n/a | — | both stated hypotheses refuted from source (prod accepts `page_size=2500`; detail and `user_visits` share one stateless gate). Needs prod to answer |
| 1033 | close | blocks-e2e | high | — | visit-columns | fixed by merged #1304 — `serialize_pipeline_row` (`data_access.py:44`) is now the single producer; parity test at `test_pipeline_row_serialization.py:206`. PR said "Refs ace#1657" so it never auto-closed |
| 1190 | close | blocks-e2e | high | — | visit-columns | duplicate of #1033, same fix, same commit. Its "related gap" paragraph is #1198's scope |
| 1183 | implement | harness | high | S | mcp-agg | `pipelines.py:185` stale `_VALID_AGGREGATIONS`; `config.py:42` already declares the single source of truth |
| 1317 | close | harness | high | — | mcp-agg | duplicate of #1183 (older, superset scope — #1183 also asks for the generate-from-the-validator remedy) |
| 1197 | implement | harness | high | S | synthetic-repoint | only `synthetic_generate_from_manifest` refreshes `visit_count`; register/repoint/uploader/provisioning leave it stale, and `context.py:96` renders it |
| 1181 | implement | harness | high | M | — | no relevance gating anywhere in `fields.py`; no `extra="forbid"` on 23 manifest models (silently swallowed `min:`/`max:`); int rounding only when HQ types the question |
| 1169 | implement | polish | high | S | — | outage half fixed by #1168 (`data_access.py:1319`); residual is fix 1 — no call site passes the opportunity the page already has, and `get_audit_session` has no parameter for it |
| 1306 | implement | polish | med | S | visit-columns | `data_access.py:102` merges `extra` before the computed merge, so a pipeline field named `opportunity_id` wins on the live path only. Prod settles the "can they differ" question by construction |
| 1184 | implement | polish | high | M | — | `llo_weekly_review.py:46` sets `showSummaryCards` and never reads it; thresholds used only for filtering; zero styling in RENDER_CODE. `performance_review.py:182` is the copyable pattern |
| 1032 | implement | polish | high | L | — | 9 claimed fixes verified present; items B–L all reproduce unchanged. C/D/F/G are one tenancy-chokepoint refactor, H is a CSP rollout |
| 1031 | implement | polish | high | L | — | **regressed: 239 open alerts, not 197** (3 crit / 75 high). Zero security bumps since the audit. Sole critical (fastmcp 3.2.0) is already inside the declared range |
| 1152 | blocked | polish | high | S | labs-capacity | the in-repo half shipped (#1153, `uvicorn_worker.py:59`); `web.json:70` carries `WEB_LIMIT_CONCURRENCY=0`. Title is stale — the RDS slot wall was removed by the 2026-08-18 `db.m6g.large` resize |
| 1298 | blocked | polish | high | n/a | labs-capacity | owner-keyed 1h cache + default-on httpx timing shipped (#1310/#1312); residual is a `Count(uservisit, distinct=True)` join in `dimagi/commcare-connect` |
| 1246 | close | polish | high | — | — | watermark-gated projection (`data_access.py:1430`, `prior_audit_projection.py:173`); production-verified in the thread (parity 0/0, 17-32x). Remaining `opportunity_id` push-down is a documented deliberate decline |

## Validation queue

**Surface: one labs deploy of current `main` + one CloudWatch / Logs-Insights session.**
Clears or advances three issues in a single visit:
- **#1162** — is `db_ms` on `/audit/api/*/{bulk-data,save,complete}/` still multi-second, and do
  `[SQL] Raw cache MISS` lines still appear inside those requests? (#1293 and #1321 merged
  2026-08-26; labs deploys are manual, so no post-fix sample exists.)
- **#1152** — peak concurrent in-flight requests and peak keep-alive sockets per uvicorn worker
  on `labs-jj-web` during a saturation episode. That one number picks `WEB_LIMIT_CONCURRENCY`.
- **#1231** — on the same visit, launch 3-5 deliberately concurrent AI-review runs and see
  whether the 73-92% error shape still reproduces.

**Surface: one production Connect request, same token, same minute.**
- **#1193** — `GET /export/opportunity/<id>/` vs `.../user_visits/` vs
  `.../user_visits/?page_size=2500` for opp 1790 or 1739. If detail 200s while user_visits
  404s, this is an upstream `dimagi/commcare-connect` bug; if all three 404, it is a
  membership change and the issue is a false alarm.

**Surface: `dimagi/commcare-connect` (another repo).**
- **#1298** — time `/export/opp_org_program_list/` against a build where
  `ProgramOpportunityOrganizationDataView`'s distinct `uservisit` count is a subquery annotation.

## Actions taken

**20 open issues → 16. 6 open PRs → 0.**

### PRs (all six resolved)

| PR | Action | Outcome |
|---|---|---|
| 1282 | merged (squash) | shipped |
| 1151 | merged (squash) | shipped |
| 953 | merged (squash) | shipped |
| 895 | merged (squash) | shipped |
| 930 | rebased on main → linter went green → merged (squash) | shipped; the red check was a stale 2026-07-21 `--all-files` run, not caused by the diff |
| 1186 | closed with evidence | superseded by merged #1118 (`65a44cda`) |

### Issues closed (4)

| # | Reason | Evidence |
|---|---|---|
| 1246 | completed | watermark-gated projection, `data_access.py:1430`; prod-verified in thread |
| 1033 | completed | merged #1304; `serialize_pipeline_row` + parity test. PR said `Refs ace#1657` so it never auto-closed |
| 1190 | not planned (duplicate of #1033) | same root cause, same commit |
| 1317 | not planned (duplicate of #1183) | unique findings ported to #1183 first |

### PRs opened (4 quick wins, cap was 5)

| PR | Issue | What |
|---|---|---|
| 1322 | Closes #1183, #1317 | MCP imports the engine's `VALID_AGGREGATIONS`; `dup_share` documented; two drift guards |
| 1323 | Refs #1169 | rung-0 opportunity hint on `get_audit_session`, threaded from the bulk page |
| 1324 | Refs #1197 | `resync_visit_count` wired into all four register/repoint paths |
| 1325 | Closes #1306 | `extra` merges last, so the framework's opportunity tag wins |

Two use `Refs`, not `Closes`, on purpose: #1169 keeps an unfixed sibling item
(`search_opportunities` returning `[]` for both "API failed" and "no access")
and wants retitling away from the incident framing; #1197 sits in a cluster with
#1034, which is still open.

### Blocked (5 commented, all left open)

#1231, #1162, #1152, #1193, #1298 — each carries the surface it needs and the
single observation that adjudicates it. `blocked-on-validation` was **not**
applied: the label does not exist in this repo and creating repo labels wasn't
in scope for the run.

### Handed back (7, over the quick-wins cap)

M: #1198, #1034, #1182, #1181, #1184. L: #1032, #1031.

`#1198` is the highest-tier implementable item in the backlog (blocks-e2e) and
was skipped only for being M — it is the obvious first target for the next run.

### Notes for the next run

- This run wrote the first YAML sidecar for this repo, so the next triage can
  carry issues forward instead of re-deriving all 20.
- `#1031` regressed since it was filed: **239** open Dependabot alerts, not 197.
- Two stale titles worth fixing so future triages don't re-derive them: #1152
  (the RDS slot wall was removed by the 2026-08-18 instance resize) and #1298
  (the "~99% our own Python" claim was disproven by the instrumentation that
  since shipped).
- One pre-existing local test failure, reproduced on clean `origin/main` and
  unrelated to anything here:
  `test_gdrive.py::test_drive_reads_are_counted_against_the_in_flight_request`.
